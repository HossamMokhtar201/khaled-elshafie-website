// PLACEHOLDER: no production domain has been assigned yet — update once the
// site is deployed (see ASSUMPTIONS.md) or set NEXT_PUBLIC_SITE_URL at build time.
export const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://khaled-elshafie.com";
